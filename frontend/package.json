{
  "name": "ai-agent-frontend",
  "version": "1.0.0",
  "description": " Multi-agent AI Assistant - Vue 3 Source",
  "scripts": {
    "dev": "vite",
    "build": "vite build --emptyOutDir && cp -f dist/index.html ../index.html && cp -f dist/index.html ../static/index.html && cp -rf dist/assets/* ../static/assets/",
    "preview": "vite preview"
  },
  "dependencies": {
    "marked": "^18.0.9",
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "vite": "^5.0.0"
  }
}
